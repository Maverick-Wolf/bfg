import 'package:flutter/material.dart';

class LmaoDed extends StatelessWidget {
  const LmaoDed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Lmao Ded"),
      ),
    );
  }
}
